<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.philipandmegan.romeoandjuliet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/jrlauncher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Profile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GenderAskActivity" />
        <activity android:name=".RegistrationAskActivity" />
        <activity android:name=".HeightAskActivity" />
        <activity android:name=".SurveyExplanationActivity" />
        <activity android:name=".EducationAskActivity"></activity>
    </application>

</manifest>